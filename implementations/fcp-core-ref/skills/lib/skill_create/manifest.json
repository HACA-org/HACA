{
  "name": "skill_create",
  "version": "1.0.0",
  "description": "Stage a new skill cartridge under workspace/stage/<name>/ for Endure installation. Optional --base <name> clones an existing skill as a starting point.",
  "timeout_seconds": 30,
  "background": false,
  "irreversible": false,
  "class": "builtin",
  "permissions": ["workspace_write"],
  "params": {
    "type": "object",
    "properties": {
      "name": {"type": "string", "description": "Name of the new skill to create."},
      "base": {"type": "string", "description": "Optional: name of an existing skill to clone as a starting point."}
    },
    "required": ["name"]
  }
}
