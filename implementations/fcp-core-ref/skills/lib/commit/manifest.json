{
  "name": "commit",
  "version": "1.0.0",
  "description": "Version-control checkpoint for workspace projects. This is the only git interface available to the CPE — direct git access is prohibited. Stages and commits the specified path within workspace_focus; optionally pushes to origin.",
  "timeout_seconds": 60,
  "background": false,
  "irreversible": true,
  "class": "builtin",
  "permissions": ["workspace_write"],
  "params": {
    "type": "object",
    "properties": {
      "path": {"type": "string", "description": "Path within workspace_focus to stage and commit."},
      "message": {"type": "string", "description": "Commit message."},
      "remote": {"type": "boolean", "description": "If true, push to origin after committing."}
    },
    "required": ["path"]
  }
}
