# §5 Boot Sequence
# Phases 0–7: distress beacon check → operator bound → host introspection →
#              crash recovery → integrity verification → skill index →
#              context assembly → critical condition check → session token
