{
  "name": "web_fetch",
  "version": "1.1.0",
  "description": "Fetch the content of a URL and return it as text. Permitted URL prefixes declared as allowlist in this manifest.",
  "timeout_seconds": 30,
  "background": false,
  "irreversible": false,
  "class": "builtin",
  "permissions": [],
  "allowlist": [],
  "max_bytes": 524288,
  "params": {
    "type": "object",
    "properties": {
      "url": {
        "type": "string",
        "description": "URL to fetch. Must match an allowlisted prefix if allowlist is non-empty."
      }
    },
    "required": [
      "url"
    ]
  },
  "execution": "script",
  "dependencies": [
    "python3"
  ]
}
