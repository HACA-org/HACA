{
  "name": "skill_create",
  "version": "1.0.0",
  "description": "Scaffold a new skill in workspace/stage/<name>/ for installation via evolution_proposal.",
  "timeout_seconds": 30,
  "background": false,
  "irreversible": false,
  "class": "builtin",
  "permissions": [
    "workspace_write"
  ],
  "params": {
    "type": "object",
    "properties": {
      "name": {
        "type": "string",
        "description": "Name of the new skill to create."
      },
      "base": {
        "type": "string",
        "description": "Optional: name of an existing skill to clone as a starting point."
      }
    },
    "required": [
      "name"
    ]
  },
  "execution": "script",
  "dependencies": [
    "python3"
  ]
}
